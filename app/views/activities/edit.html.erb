<div class="span12">
  <h1><%= "#{t('action.edit')} #{Activity.model_name.human}".mb_chars.titlecase.to_s %></h1>
  <br>

  <%= render 'form' %>

  <%= link_to "#{t('action.show')}", @activity %> |
  <%= link_to "#{t('action.back')}", activities_path %>
</div>
