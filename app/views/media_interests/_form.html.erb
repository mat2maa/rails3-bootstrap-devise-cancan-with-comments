<%= simple_form_for(@media_interest) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :user_profile_id %>
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
