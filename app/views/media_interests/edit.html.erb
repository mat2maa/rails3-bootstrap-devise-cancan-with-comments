<div class="span12">
  <h1><%= "#{t('action.edit')} #{MediaInterest.model_name.human}".mb_chars.titlecase.to_s %></h1>
  <br>

  <%= render 'form' %>

  <%= link_to "#{t('action.show')}", @media_interest %> |
  <%= link_to "#{t('action.back')}", media_interests_path %>
</div>
