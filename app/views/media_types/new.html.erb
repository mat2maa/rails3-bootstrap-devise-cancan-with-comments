<div class="span12">
  <h1><%= "#{t(:new, :gender => :m)} #{MediaType.model_name.human}".mb_chars.titlecase.to_s %></h1>
  <br>

  <%= render 'form' %>

  <%= link_to "#{t('action.back')}", media_types_path %>
</div>
